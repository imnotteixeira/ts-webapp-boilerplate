// Anything in this file can be accessible by Backend as well as Frontend

interface AppProps {
    // Add something here to define 
    // the structure of what will be sent to frontend
}

interface AppContext {
    initialState: AppProps
}